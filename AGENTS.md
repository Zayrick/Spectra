# 仓库协作约束

## 无状态交付

- 每次修改都让仓库直接呈现当前需求下应有的最终状态，不在产物中记录对话、修改过程、纠错经过或废弃方案。
- 用户否定或要求删除某项内容时，删除该内容及其全部连带残留；不得把“没有该内容”包装成新特性、名称、注释、文档、配置项或测试。
- 名称、结构、文档和注释只描述现有职责与行为。变更历史属于 Git 历史；除非用户明确要求，不在交付物中编写补丁说明、迁移叙事或决策复盘。

## 范围控制

- 只实现用户明确要求的结果，以及使该结果正确、可运行、可验证所必需的改动。
- 不主动加入相邻功能、可选模式、兼容层、回退路径、额外抽象、配置开关或“以后可能有用”的扩展点。
- 不因修改了代码就机械扩写文档、示例、测试或配置；只更新会因本次修改而失真或确有必要覆盖的部分。
- 可选方案会改变产品行为、公共接口或任务边界时，先取得用户确认。

## 删除与收口

- 删除功能或撤回错误实现时，全仓搜索相关标识符、类型、分支、依赖、测试、示例、注释和文档，并一并清理。
- 不保留无调用方的适配器、弃用别名、空壳模块、死分支或仅用于解释旧实现的注释。
- 不使用 `without-*`、`no-*`、`legacy-*` 等否定式命名纪念已删除内容；改用能直接表达当前职责的名称。
- 不为“某个已撤回方案不存在”增加测试。只有当禁止该行为本身是当前公共契约、安全边界或回归风险时，才验证其不可用。

## 文档与注释

- 文档面向当前使用者，直接说明现有能力、接口、操作和必要限制。
- 只有在影响正确使用、兼容性或安全性时才说明不支持项；不得罗列与当前目标无关的缺失功能。
- 注释解释代码无法直接表达的协议约束、业务不变量或原因，不复述代码，也不解释曾经为什么采用或删除另一种方案。
- 示例必须与当前界面和 API 一致，不引用未随仓库交付的本地资料；正式产物不得保留占位值。

## 完成检查

- 提交前审阅完整 diff，确认每个改动都能由当前任务直接解释。
- 对被删除或更名的概念执行全仓搜索，确认没有语义残留，而不只检查同名文本。
- 按项目现有工具运行格式化、静态检查和测试；最终文档、示例与实际行为互相一致。

## 协议资产隔离

- 未获公开授权的第三方设备标识、报文格式和驱动实现不得进入 Git 索引，也不得出现在源码、测试、示例、文档或注释中。
- 本地协议插件必须存放在由目录级 `.gitignore` 以 `*` 整体排除的私有插件目录；不得为测试或说明复制其中的实现细节。
- 交付前使用 `git ls-files` 和全仓搜索确认私有资产及其可识别残留未被跟踪。
